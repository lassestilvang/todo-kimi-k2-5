[test]
preload = ["./src/lib/db/__tests__/setup.ts"]
coverage = true
coverageReporter = ["text", "lcov"]

[install]
# Ensure native dependencies are built correctly
native = true

[install.scopes]
# Any scoped package configurations if needed